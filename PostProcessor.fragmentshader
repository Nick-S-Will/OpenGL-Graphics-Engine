#version 330

in vec2 retTexCoords;

uniform sampler2D screenTexture;

out vec4 FragColor;

void main()
{
	vec3 color = vec3(1.f) - texture(screenTexture, retTexCoords).rgb;
	FragColor = vec4(color, 1.f);
}